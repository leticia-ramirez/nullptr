[tool.poetry]
name = "mobile"
version = "0.1.0"
description = "zonaroja"
authors = ["NotMESanjines <msanjines@fi.uba.ar>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
kivy = "^2.3.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
